#include "AI.h"

Move AI::executeMove(sf::RenderWindow* window, Board* board)
{
    return Move();
}

Move AI::getBestMove(Game::GameState* t_currentState, Board* board, int t_depth, Move t_move)
{
    return Move();
}
